<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF7F7F8"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/projectTabLayout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_45"
        android:background="@color/colorPrimary"
        app:tabGravity="center"
        app:layout_collapseMode="pin"
        app:tabIndicatorFullWidth="true"
        app:tabIndicatorHeight="@dimen/dp_0_5"
        app:tabMaxWidth="0dp"
        app:tabMode="scrollable"
        app:tabRippleColor="@null"
        app:tabIndicatorColor="@color/text_blue"
        app:tabBackground="@drawable/tab_bg_selector"
        app:tabIndicator="@drawable/shape_tab_indicator_blue"
        app:tabSelectedTextColor="@color/text_blue"
        app:tabTextAppearance="@style/tabLayout_Text_color"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/projectViewPager2"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/projectTabLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>